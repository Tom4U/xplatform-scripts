/**
 * Function for demo purposes
 * @param arg1 First number
 * @param arg2 Second number
 * @returns Sum of arg1 and arg2
 */
export function init(arg1: number, arg2: number): number {
    return arg1 + arg2;
}